# Proofs

Proof nodes for Stewart Calculus Chapter 1 — Functions and Models.

Each proof tracks which statements it uses (dependency edges) and which
statement it proves. See `AGENTS.md` for proof schema rules.
